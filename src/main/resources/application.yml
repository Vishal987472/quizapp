spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/quizapp
    username: postgres
    password: vishal

  jpa:
    open-in-view: false
    defer-datasource-initialization: true

    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

jwt:
  secret: mySuperSecretKeymySuperSecretKey12345


server:
  port: 8080